# ===================================================================
# ShopAI Backend Configuration (AI-Powered E-Commerce Platform)
# ===================================================================
spring.application.name=shopai-backend
server.port=8082

# ===================================================================
# MongoDB Database Configuration
# ===================================================================
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://Nazneen:Naznin@cluster0.hr32he0.mongodb.net/shopaidb?retryWrites=true&w=majority}
spring.data.mongodb.database=${MONGODB_DATABASE:shopaidb}
spring.data.mongodb.auto-index-creation=false

# ===================================================================
# Security & JWT Configuration
# ===================================================================
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
# 24 Hours in milliseconds = 86400000
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ===================================================================
# CORS Configuration
# ===================================================================
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://localhost:4173}

# ===================================================================
# Business Rules Configuration
# ===================================================================
app.delivery.free-threshold=500.0
app.delivery.standard-fee=40.0
app.pickup.default-slot-capacity=10
app.returns.eligibility-days=7

# ===================================================================
# ShopAI AI Engine Configuration (Gemini / Heuristic Engine)
# ===================================================================
app.ai.gemini.api-key=${GEMINI_API_KEY:}
app.ai.gemini.model=${GEMINI_MODEL:gemini-1.5-flash}
